// Project-level configuration
// Set CONTACT_FORM_ACTION to your Formspree endpoint (e.g. https://formspree.io/f/XXXXX)
// If empty, the contact form will fallback to opening the user's mail client via mailto: to CONTACT_EMAIL.
export const CONTACT_FORM_ACTION = 'https://formspree.io/f/manlgddd';
export const CONTACT_EMAIL = 'antoverdusal@gmail.com';

const CONFIG = {
  CONTACT_FORM_ACTION,
  CONTACT_EMAIL,
};

export default CONFIG;
